<h1>Reviewer: <%= @review.author %></h1>
<h2>Product: <%= @product.name %></h2>
<h3>Opinion about the product: <%= @review.content_body %></h3>
<h3>Rating from 1-5:<%= @review.rating %></h3>

<%= button_to "Edit Review", 
              edit_product_review_path(@product, @review), 
              method: :get %>

<%= button_to "Delete Review", 
              product_review_path(@product, @review),
              method: :delete %>

<%= button_to "Return to product", 
              product_path(@product), 
              method: :get %>